# MyRTOS 需求追踪矩阵

> 目的：把用户原始目标拆成可验证需求，防止后续实现缩水。  
> 状态：草案，随实现推进更新证据列。  
> 规则：每项需求必须有设计位置、实现文件、测试证据、文档位置四类证据；未齐全前不能声明目标完成。

| 编号 | 原始需求 | 可验证要求 | 设计证据 | 实现证据 | 测试证据 | 文档证据 | 当前状态 |
|------|----------|------------|----------|----------|----------|----------|----------|
| R-001 | 参考 FreeRTOS 源码 | 只参考公开架构和模块划分，不复制源码、注释、手册原文 | `docs/superpowers/specs/2026-06-07-myrtos-c-scope-design.md` 第 1、10 节 | 源码和手册使用原创 `MRT_` API 与原创中文说明；未使用 FreeRTOS 风格 public API 名 | `python tools\verify\check_original_symbols.py` 通过：未发现 banned FreeRTOS-style public symbols | `docs/manual/MyRTOS_Reference_Manual_zh.md` 第 1 节说明原创边界 | 部分验证 |
| R-002 | 复现类似嵌入式 RTOS | 提供任务、调度、同步、通信、定时、内存、低功耗、trace、移植层 | 设计草案第 3、4 节 | Foundation + scheduler + task lifecycle APIs + queues + semaphores + mutexes + event groups + task notifications + software timers + service command queue + stream/message buffers + heap + fixed memory pool + dynamic queue/task/sync/event/timer/buffer allocation + tickless idle + trace hook + runtime stats + assert hook 已实现；互斥锁 timeout 优先级回滚和持锁任务删除拒绝策略已实现；STM32/DSP 端口契约 helper 已实现；任务 TCB 运行期栈顶保存/恢复契约已实现；新增端口源码见 `include/myrtos/portable/mrt_port_stm32_cm.h`、`src/portable/stm32_cm/mrt_port_stm32_cm.c`、`src/portable/stm32_cm/mrt_port_stm32_cm_context.S`、`include/myrtos/portable/mrt_port_dsp_c28x.h`、`src/portable/dsp_c28x/mrt_port_dsp_c28x.c`；新增 smoke 工程见 `examples/stm32/`、`examples/dsp/`；硬件 smoke 日志 helper 和完整报告 emitter 见 `examples/hardware_smoke/` | `python tools\run_host_tests.py`：72 个 host 测试目标通过；`python tools\verify\check_embedded_smoke_projects.py` 通过，STM32 cross build 和 DSP model smoke 均完成 | `docs/manual/MyRTOS_Reference_Manual_zh.md` 覆盖任务 API、同步通信 API、内存、timer service 命令队列、tickless/trace/runtime stats/assert、STM32/DSP 详细移植步骤、示例 smoke 工程和动态对象生命周期 | 部分实现 |
| R-003 | 适应 STM32 | 提供 Cortex-M4/M7 ARM GCC/CMake 移植层与示例 | 设计草案第 2、6、7.4 节 | 已实现 host-testable Cortex-M 栈帧、SysTick reload、BASEPRI 编码 helper，以及 MPU 区域规整 helper；新增 `examples/stm32/main.c`、`examples/stm32/mrt_port_stm32_smoke.c`、`examples/stm32/startup_stm32cm.c`、`examples/stm32/linker.ld`、`examples/stm32/runtime_stubs.c` 作为 ARM GCC smoke scaffold；`main.c` 已把 `MRT_PortStm32CmInitializeStack()` 返回的初始 PSP 写回任务 TCB | `test_port_stm32_stack`、`test_port_stm32_tick_priority`、`test_port_stm32_mpu`、`test_stm32_context_scaffold.py` 通过；`python tools\verify\check_embedded_smoke_projects.py` 交叉编译 STM32 smoke ELF 成功 | `docs/manual/MyRTOS_Reference_Manual_zh.md` 第 5 节包含 STM32 工具链、移植前准备、工程分层、启动文件、向量表、SysTick、PendSV/SVC、优先级、MPU、链接脚本、首次联调、示例 smoke 工程和排错步骤 | 部分实现 |
| R-004 | 适应 DSP | 提供 DSP 抽象端口、mock 测试，后续可替换真实 DSP 汇编端口 | 设计草案第 2、6、7.4 节 | 已实现 DSP C28x 风格栈帧 helper 与软件中断上下文切换模型；新增 `examples/dsp/main.c`、`examples/dsp/mrt_port_dsp_model.c` 作为 host-verifiable smoke/model；新增 `src/portable/dsp_c28x/mrt_port_dsp_c28x_context.asm` 作为 C28x 风格首任务启动、软件中断 yield、寄存器保存/恢复和 C 钩子交接汇编骨架；新增 `examples/dsp/startup_c28x.c`、`examples/dsp/mrt_port_dsp_c2000_smoke.c`、`examples/dsp/linker_c28x.cmd` 作为 C2000 启动、向量、timer/software interrupt/ADC glue 和 RTOS 分区骨架；真实 DSP 汇编仍需按具体型号和编译器 ABI 校正并上板补证 | `test_port_dsp_stack`、`test_port_dsp_context` 通过；`python tests\static\test_dsp_context_scaffold.py` 通过；`python tests\static\test_dsp_c2000_project_scaffold.py` 通过；`python tools\verify\check_embedded_smoke_projects.py` 编译并运行 DSP model smoke 成功且检查 C2000 骨架文件存在；真实 DSP smoke test 待具体 DSP 型号和工具链补证 | `docs/manual/MyRTOS_Reference_Manual_zh.md` 第 6 节包含 DSP 工具链、移植前准备、工程分层、ABI、栈帧、上下文汇编骨架、C2000 启动/链接/ISR glue 骨架、tick、软件中断切换、首次联调、嵌套中断、示例 smoke/model 和排错步骤 | 部分实现 |
| R-005 | 中文专业详细注释 | 每个函数含功能、参数、返回值、调用示例、注意事项 | 设计草案第 8 节 | `include/`、`src/`、`examples/` 与 `tests/` 函数注释已补齐；本阶段补充 `src/kernel/mrt_task_internal.h`、`src/kernel/mrt_queue.c`、`src/kernel/mrt_task.c` 以及 `tests/unit/test_types_contract.c` 缺口 | `python tools\verify\check_chinese_comments.py` 通过：include/src/examples/tests function comments covered | `docs/manual/MyRTOS_Reference_Manual_zh.md` 第 7.4 节列出注释/验证命令 | 部分验证 |
| R-006 | 函数内部逐行注释 | 每个函数关键语句有中文步骤注释；复杂分支逐行解释 | 设计草案第 8 节 | `include/`、`src/`、`examples/` 与 `tests/` 函数体附近中文步骤注释通过静态扫描；历史测试文件不纳入本阶段静态范围 | `python tools\verify\check_chinese_comments.py` 通过 | `docs/manual/MyRTOS_Reference_Manual_zh.md` 第 7.4 节列出注释/验证命令 | 部分验证 |
| R-007 | 类 FreeRTOS 官方说明手册 | 提供原创中文参考手册，API 按章节列原型/参数/返回值/示例/限制 | 设计草案第 9、10 节 | 不适用 | `python tools\verify\check_api_manual_coverage.py` 通过：135 API section(s) covered；`python tools\verify\check_api_catalog_prototypes.py` 通过：135 API prototype(s) aligned | `docs/manual/MyRTOS_Reference_Manual_zh.md` 已写，包含关于本手册、API 使用规则、API 参考、移植步骤、附录 | 部分验证 |
| R-008 | 所有功能必须测试 | 每个 public API 和核心内部算法有自动化测试 | 设计草案第 7 节 | Foundation、scheduler、task lifecycle、task TCB stack_top、queue、semaphore、mutex、event group、task notification、software timer、timer service command queue、stream buffer、message buffer、heap、memory pool、dynamic queue/task/sync/event/timer/buffer allocation、dynamic stream/message buffer delete、stream/message buffer writer wait/delete busy、tickless、trace、runtime stats、assert、STM32 port helper、DSP port helper public API、硬件 smoke 日志格式 helper、完整报告 emitter 已有 host/sim/coupling 测试；新增 STM32/DSP smoke project 目录和 DSP 汇编骨架静态检查 | `python tools\run_host_tests.py`：72 个 host 测试目标通过；`python tests\static\test_dsp_context_scaffold.py` 通过；`python tools\verify\check_embedded_smoke_projects.py` 通过；`python tools\verify\check_chinese_comments.py` 已扩展到 `include/src/examples/tests`；`test_hardware_smoke_log` 覆盖硬件 smoke helper 输出格式、整数转换和非法参数；`test_hardware_smoke_report` 覆盖 STM32/DSP 必填字段全集输出和非法参数 | `docs/verification/final_verification_report.md` 记录最新 host/static/smoke 结果 | 部分验证 |
| R-009 | 所有耦合情况测试清楚 | 用耦合矩阵覆盖模块交互、ISR、超时、内存失败、trace、tickless | 设计草案第 7.3 节；耦合矩阵文件 | 已验证 scheduler + task lifecycle + tick + ready/delay list、task + heap dynamic allocation、task TCB stack_top + scheduler/PendSV save contract、STM32 initial PSP + TCB stack_top smoke wiring、task ResumeFromISR、queue + task/ISR、queue + heap dynamic allocation、dynamic semaphore/mutex/event/timer/buffer heap failure and delete semantics、dynamic stream/message buffer delete heap release、stream/message buffer writer wait/delete busy、semaphore + task/ISR、mutex + priority inheritance/ownership/timeout rollback/held-owner delete policy、event group + multi-waiter/clear/ISR、task notification + overwrite/wait/ISR、software timer + service command queue、software timer + kernel tick expiry event queue、software timer + pending function FIFO、software timer + tickless compensation、stream buffer + wrap/ISR reader wake、message buffer + packet capacity/ISR reader wake、heap allocation failure、heap coalescing、trace + task switch、trace + queue、runtime stats + current task/tick/scheduler switch、assert hook、STM32 stack/tick/BASEPRI helper、STM32 MPU layout helper、DSP stack/context helper、STM32 smoke scaffold、DSP smoke/model、硬件 smoke raw-log schema 对齐、硬件 smoke 日志格式 helper、硬件 smoke 完整报告 emitter、硬件 smoke 目标级预检、硬件 smoke 原始日志直检；真实板级端口仍待后续模块 | `test_task_stack_top` 已加入 TCB 栈顶与调度切换保存契约证据；`test_stm32_context_scaffold.py` 已加入 STM32 initial PSP 写回 TCB 静态证据；`test_timer_service_task` 已加入定时器服务命令队列耦合证据；`test_buffer_dynamic_allocation` 已加入 writer wait/delete busy 耦合证据；`python tools\run_host_tests.py`：72 个 host 测试目标通过；`python tools\verify\check_embedded_smoke_projects.py` 通过；`python tests\static\test_hardware_smoke_raw_log_schema.py` 通过；`test_hardware_smoke_log`、`test_hardware_smoke_report`、`test_hardware_smoke_raw_log_checker.py` 和 `test_hardware_smoke_capture_runner.py` 通过 | `docs/verification/coupling_test_matrix.md` 更新 C-017、C-018、C-019、C-023、C-028、C-029、C-035、C-036、C-037、C-040、C-041、C-042、C-043、C-044 的证据 | 部分验证 |
| R-010 | C 版大而全范围 | 包含 stream/message buffer、tickless、trace、MPU 预留、多 heap 策略等高级模块 | 设计草案第 4 节 | stream/message buffer 与动态创建/删除路径已实现；多 heap 策略已实现线性、free-list、coalescing 三种模式；固定块 memory pool、动态队列、动态任务和动态同步/定时对象堆分配已实现；tickless idle、trace hook、runtime stats、assert hook、定时器服务命令队列已实现；STM32 MPU 区域规整 helper 已实现并有 host 测试；真实板级 MPU 端口仍待后续计划 | `test_timer_service_task`、`test_stream_buffer_create_static`、`test_stream_buffer_send_receive`、`test_stream_buffer_isr`、`test_stream_buffer_isr_wakes_reader`、`test_message_buffer_create_static`、`test_message_buffer_send_receive`、`test_message_buffer_isr`、`test_buffer_dynamic_allocation`、`test_heap_linear`、`test_heap_free_list`、`test_heap_coalescing`、`test_memory_pool`、`test_queue_dynamic_allocation`、`test_tickless_expected_idle`、`test_tickless_timer_compensation`、`test_trace_task_switch`、`test_trace_queue`、`test_runtime_stats`、`test_assert_hook`、`test_port_stm32_mpu` 通过 | `docs/manual/MyRTOS_Reference_Manual_zh.md` 覆盖高级模块 API、timer service 命令队列、运行统计、动态对象限制和 STM32 MPU 布局步骤 | 部分实现 |
| R-011 | 手册包含详细移植步骤 | STM32 与 DSP 移植章节必须覆盖工具链、启动文件、向量表、tick、上下文切换、栈布局、临界区、低功耗、示例、排错和真实板级证据归档 | 设计草案第 9 节；`docs/api/myrtos_api_catalog.md` 第 14 节 | 不适用 | `python tools\verify\check_api_manual_coverage.py` 验证 STM32/DSP 移植关键词覆盖；`python tools\verify\check_embedded_smoke_projects.py` 验证手册中的 smoke 工程步骤可被实际目录和构建命令支撑；`python tests\static\test_dsp_context_scaffold.py` 验证 DSP 汇编骨架和边界说明；`python tests\static\test_hardware_smoke_preflight.py` 验证硬件 smoke 预检配置规则；`python tests\static\test_hardware_smoke_capture_runner.py` 验证采集执行器 dry-run/execute 契约；`python tests\static\test_hardware_smoke_raw_log_schema.py` 验证原始日志 schema 与生成器、文档、C 头文件、完整报告 emitter、raw-log 直检脚本和指南对齐；`python tests\static\test_hardware_smoke_raw_log_checker.py` 验证原始 UART/trace 日志直检规则；`test_hardware_smoke_log` 验证板级日志输出 helper 的 `Key: Value` 行格式、十进制整数和非法参数路径；`test_hardware_smoke_report` 验证板级完整报告 emitter 的 STM32/DSP 必填字段全集和非法参数路径；`python tests\static\test_hardware_smoke_evidence_checker.py` 验证真实板级证据 checker 规则并覆盖 raw-log SHA-256 错配拒绝；`python tests\static\test_hardware_smoke_evidence_generator.py` 验证原始 UART/trace 日志到最终证据文件的生成器规则并覆盖 `Raw-Log-Path`/`Raw-Log-SHA256` 派生 | `docs/manual/MyRTOS_Reference_Manual_zh.md` 第 5 节“STM32 Cortex-M 移植步骤”和第 6 节“DSP 移植步骤”包含移植前准备、工程分层、关键接入顺序、从厂商裸机工程迁入 MyRTOS 的实际顺序、首次联调、板级验收、真实板级证据归档、详细迁移流程、工程接入点、smoke 工程、示例、上下文汇编骨架和排错；`docs/verification/hardware_smoke/raw_log_schema.md` 给出 STM32/DSP 原始日志字段 schema；`examples/hardware_smoke/mrt_hardware_smoke_log_schema.h` 给出板级 C 输出端可复用字段常量；`examples/hardware_smoke/mrt_hardware_smoke_log.h` 给出不依赖 `printf` 的 `Key: Value` 输出 helper；`examples/hardware_smoke/mrt_hardware_smoke_report.h` 给出 STM32/DSP 必填字段全集输出 helper；`docs/verification/hardware_smoke/hardware_smoke_preflight.json` 和 `docs/verification/hardware_smoke/collection_checklist.md` 给出 STM32/DSP 实机日志采集前置配置与采集步骤；`tools/verify/check_hardware_smoke_preflight.py` 检查目标配置、运行时长和必需字段；`tools/verify/check_hardware_smoke_raw_log_schema.py` 检查字段契约漂移并覆盖 raw-log 直检脚本字段；`tools/verify/check_hardware_smoke_raw_log.py` 直接检查原始 UART/trace 日志字段、PASS 状态和数值规则；`tools/verify/run_hardware_smoke_capture.py` 预演或执行编译、烧录、采集、原始日志直检、证据生成和目标级证据校验；`tools/verify/generate_hardware_smoke_evidence.py` 支持从原始日志生成最终证据并写入 SHA-256；`tools/verify/check_hardware_smoke_evidence.py` 校验最终证据和原始日志哈希匹配；`docs/verification/hardware_smoke/*.template.md` 提供 STM32/DSP 实机日志模板；第 7.4 节列出 release、预检、schema、原始日志直检、采集执行器与硬件证据 gate 命令 | 部分验证 |

## 最新增量证据：STM32 SVC/PendSV 汇编骨架

- `R-003`：新增 `src/portable/stm32_cm/mrt_port_stm32_cm_context.S`，提供 `SVC_Handler`、`PendSV_Handler` 和 `MRT_PortStm32CmStartFirstTaskAsm` 的 Cortex-M 汇编入口骨架；`python tests\static\test_stm32_context_scaffold.py` 验证符号、PSP/R4-R11 保存恢复标记、C 钩子接线、`MRT_TaskKernelSwitchStackTop()` 接入，以及 `examples/stm32/main.c` 的初始 PSP 写回 TCB 接线；`test_task_stack_top` 验证 TCB 栈顶保存/恢复契约。
- `R-008`：`tools/verify/run_release_verification.py` 默认链路新增 `dsp-c2000-project-scaffold` 步骤，默认 release 验证从 13 步扩展为 14 步。
- `R-009`：`python tools\verify\check_embedded_smoke_projects.py` 已把 `.S` 文件、`-Isrc/kernel` 内部契约路径、初始 PSP 写回 TCB 接线纳入 ARM GCC 交叉编译，证明 STM32 smoke ELF 同时覆盖 C 端口、汇编入口骨架和 TCB 栈顶 helper 接线。
- `R-011`：手册第 5 节已补充 `.S` 文件角色、SVC/PendSV C 钩子说明、`stack_top`/PSP 保存恢复步骤和真实板级 smoke 边界，继续明确真实板级 smoke 证据不能由交叉编译替代。
- `R-011`：手册第 5.6、6.6、7.4 节新增硬件 smoke 预检配置步骤，`hardware_smoke_preflight.json` 和 `check_hardware_smoke_preflight.py` 用于采集前确认 STM32/DSP 芯片、板卡、命令、最短运行时长和必需字段。
- `R-011`：最终硬件证据新增 `Raw-Log-Path` 和 `Raw-Log-SHA256` 规则，生成器自动派生，checker 读取原始日志并拒绝 SHA-256 错配。
- `R-011`：新增 `run_hardware_smoke_capture.py` 和 `test_hardware_smoke_capture_runner.py`，用于 dry-run 或执行预检、编译、构建、烧录、采集、证据生成和目标级证据校验流水线。
- `R-009`/`R-011`：`check_hardware_smoke_preflight.py` 和 `run_hardware_smoke_capture.py` 支持目标级预检过滤；`--target STM32` 或 `--target DSP` 只检查当前目标，`all` 仍检查两类目标并拒绝任一占位配置。
- `R-011`：新增 `raw_log_schema.md`、`mrt_hardware_smoke_log_schema.h`、`mrt_hardware_smoke_log.h`、`mrt_hardware_smoke_report.h`、`check_hardware_smoke_raw_log_schema.py`、`test_hardware_smoke_raw_log_schema.py`、`test_hardware_smoke_log.c` 和 `test_hardware_smoke_report.c`，把 STM32/DSP 原始 UART/trace 字段契约、`Key: Value` 输出格式和必填字段全集 emitter 集中校验；默认 release 验证保持 15 步，host 测试扩展为 72 个目标。
- `R-009`/`R-011`：`check_hardware_smoke_raw_log_schema.py` 现在把 `mrt_hardware_smoke_report.h` 纳入字段漂移检查，确保生成器新增 STM32/DSP 必填字段时，完整报告 emitter 也必须引用对应 `MRT_SMOKE_FIELD_*` 常量。
- `R-009`/`R-011`：新增 `check_hardware_smoke_raw_log.py` 和 `test_hardware_smoke_raw_log_checker.py`，在最终证据生成前直接检查原始 UART/trace 字段、PASS 状态、运行时长、断言次数和 heap 最小剩余量；`run_hardware_smoke_capture.py` 现在在 capture 后、generate evidence 前执行该检查。
- `R-004`/`R-011`：新增 `src/portable/dsp_c28x/mrt_port_dsp_c28x_context.asm` 和 `tests/static/test_dsp_context_scaffold.py`，记录 DSP 首任务启动、软件中断 yield、寄存器保存/恢复和真实移植边界。
- `R-004`/`R-011`：新增 `examples/dsp/startup_c28x.c`、`examples/dsp/mrt_port_dsp_c2000_smoke.c`、`examples/dsp/linker_c28x.cmd` 和 `tests/static/test_dsp_c2000_project_scaffold.py`，记录 C2000 启动向量、CPU Timer0 tick、软件中断、ADC/DMA ISR、RTOS heap、任务栈、DMA buffer、trace buffer 分区和真实移植边界。

## 完成判定

目标完成必须同时满足：

- 所有 `R-*` 行状态为“已验证”。
- 每行实现证据指向具体源码文件。
- 每行测试证据指向通过的测试命令和结果。
- 每行文档证据指向手册具体章节。
- 测试报告记录 host、耦合、端口 mock、嵌入式 smoke test 结果。

## 关联规格

- API 目录：`docs/api/myrtos_api_catalog.md`
- 测试套件计划：`docs/verification/test_suite_plan.md`
- 耦合测试矩阵：`docs/verification/coupling_test_matrix.md`
